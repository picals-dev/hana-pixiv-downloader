# AGENTS.md

picals-crawler — 开箱即用的 Pixiv 图片下载 CLI 工具。Rust, edition 2024。

## Rules

1. **不做过度实现**。只写当前需求明确要求的代码，不为「未来可能需要」预留抽象层或泛型参数。
2. **中文优先**。面向用户的字符串（CLI 输出、错误信息、`--help` 文本）用中文。代码注释用中文。模块文档（`//!`）用中文。
